# 新手入门指南

本指南专门为**第一次使用 Claude Code** 和 **Claude Context Manager** 的用户编写，从零开始一步步教你完成配置。

## 前置知识

### 什么是 Claude Code？

Claude Code 是 Anthropic 公司推出的 AI 编程助手命令行工具。它可以：
- 帮助你编写和修改代码
- 回答编程相关问题
- 执行各种开发任务

官方文档：https://docs.anthropic.com/en/docs/claude-code

### 什么是斜杠命令？

在 Claude Code 中，以 `/` 开头的是斜杠命令，例如：
- `/help` - 显示帮助
- `/clear` - 清除对话
- `/save-context` - 保存上下文（本工具提供）

### 什么是 ~/.claude 目录？

`~` 表示你的用户主目录：
- macOS: `/Users/你的用户名/`
- Linux: `/home/你的用户名/`

所以 `~/.claude` 就是 `/Users/你的用户名/.claude`（macOS）或 `/home/你的用户名/.claude`（Linux）。

这是 Claude Code 的**用户级配置目录**，存放全局配置、命令、技能等。

## 第一步：确认环境

### 1.1 检查 Claude Code 是否已安装

打开终端，输入：

```bash
claude --version
```

如果显示版本号，说明已安装。如果提示"command not found"，请先安装 Claude Code。

### 1.2 检查 ~/.claude 目录

```bash
ls -la ~/.claude
```

**情况 A**：如果显示目录内容，说明目录已存在，可以继续。

**情况 B**：如果提示"No such file or directory"，运行一次 Claude Code 让它自动创建：

```bash
claude
# 进入后输入 /exit 退出
```

或手动创建：

```bash
mkdir -p ~/.claude
```

## 第二步：下载项目

### 方式一：使用 Git（推荐）

```bash
# 进入你想存放项目的目录
cd ~/Downloads  # 或其他你喜欢的目录

# 克隆项目
git clone https://github.com/gaoziman/claude-context-manager.git

# 进入项目目录
cd claude-context-manager
```

### 方式二：直接下载 ZIP

1. 访问 https://github.com/gaoziman/claude-context-manager
2. 点击绿色的 "Code" 按钮
3. 选择 "Download ZIP"
4. 解压到任意目录
5. 在终端中进入该目录

## 第三步：运行安装脚本

### 3.1 添加执行权限

```bash
chmod +x install.sh
```

**解释**：`chmod +x` 是给文件添加"可执行"权限，这样才能运行脚本。

### 3.2 运行安装脚本

```bash
./install.sh
```

**解释**：`./` 表示"当前目录下的"，所以 `./install.sh` 就是运行当前目录下的 install.sh 脚本。

### 3.3 安装过程

你会看到类似这样的输出：

```
╔═══════════════════════════════════════════════════════════════════╗
║       🧠 Claude Context Manager - Installation                    ║
╚═══════════════════════════════════════════════════════════════════╝

[Step 1] Checking requirements...
✓ Operating system: Darwin
✓ Found existing ~/.claude directory

[Step 2] Checking for existing configuration...
✓ No existing context manager files found

[Step 3] Installing Claude Context Manager...
  Installing commands...
✓ save-context.md
✓ load-context.md
✓ list-contexts.md
✓ search-context.md
  Installing skills...
✓ SKILL.md

╔═══════════════════════════════════════════════════════════════════╗
║                    ✅ Installation Complete!                      ║
╚═══════════════════════════════════════════════════════════════════╝
```

## 第四步：验证安装

### 4.1 检查文件是否安装成功

```bash
# 检查命令文件
ls ~/.claude/commands/ | grep context

# 应该看到：
# list-contexts.md
# load-context.md
# save-context.md
# search-context.md
```

```bash
# 检查技能文件
ls ~/.claude/skills/context-manager/

# 应该看到：
# SKILL.md
```

### 4.2 在 Claude Code 中验证

**重要：必须重启 Claude Code！**

1. 如果 Claude Code 正在运行，先退出（输入 `/exit` 或关闭终端）
2. 重新打开终端
3. 启动 Claude Code：`claude`
4. 输入 `/` 字符

你应该能看到新增的命令：
- `/save-context`
- `/load-context`
- `/list-contexts`
- `/search-context`

## 第五步：开始使用

### 5.1 保存上下文

当你完成一些重要的工作后：

```
/save-context 我的第一次保存
```

### 5.2 查看保存的会话

```
/list-contexts
```

### 5.3 加载之前的上下文

```
/load-context 1
```

## 理解安装原理

### 项目文件 vs 安装位置

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   📦 你下载的项目目录                    🏠 安装目标目录          │
│   ~/Downloads/claude-context-manager/   ~/.claude/              │
│                                                                 │
│   这个目录是"源文件"                     这个目录是"安装位置"     │
│   安装后可以删除                          必须保留               │
│                                                                 │
│   .claude/commands/save-context.md       commands/save-context.md│
│            ↓                                     ↑               │
│            └────── install.sh 复制到 ───────────┘               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 为什么安装到 ~/.claude？

Claude Code 有两种配置级别：

| 级别 | 目录位置 | 作用范围 |
|------|----------|----------|
| **用户级** | `~/.claude/` | 所有项目都能使用 |
| 项目级 | `项目目录/.claude/` | 只在该项目中使用 |

我们的工具安装到**用户级目录**，所以：
- 你在任何项目中都能使用 `/save-context`
- 所有会话都统一保存在 `~/.claude/conversations/`

## 常见问题

### Q: 安装后看不到命令？

**A**: 必须重启 Claude Code。完全关闭后重新打开。

### Q: 提示权限不足？

**A**: 运行 `chmod +x install.sh` 添加执行权限。

### Q: ~/.claude 目录不存在？

**A**: 先运行一次 `claude` 命令，或手动创建 `mkdir -p ~/.claude`。

### Q: 安装后项目目录可以删除吗？

**A**: 可以。安装脚本已经把需要的文件复制到 `~/.claude/` 了。但建议保留，方便以后升级。

## 下一步

- 阅读 [使用指南](./usage.md) 了解详细用法
- 阅读 [最佳实践](./best-practices.md) 了解高效使用方法
- 遇到问题查看 [常见问题](./faq.md)

---

如果你成功完成了以上步骤，恭喜你！🎉 现在可以开始使用 Claude Context Manager 来管理你的会话上下文了。
